# HANDOFF

最後更新：2026-08-09

目前有兩條工作線：**（A）三檔 ETF 補齊**（新，見下一節，程式已寫完並唯讀驗證，
還沒寫進 DB）、**（B）日期錯位第二批**（見「第二批」一節，狀態仍是 ATTEMPT、等獨立審查）。

## 第三批：補完最後 3 檔純台股主動式 ETF（2026-08-09）

追蹤缺口歸零：**22 檔純台股主動式 ETF 全部有 scraper**（TWSE `STOCK_DAY_ALL` 重查：
上市 `00xxxA` 29 檔 − 7 檔海外型 ＝ 22 檔）。新增三家投信、三支新 scraper：

| ETF | 投信 | 新模組 | 取得方式 | 資料日期錨點 |
| --- | --- | --- | --- | --- |
| 00996A | 兆豐 | `src/megafunds_scraper.py` | `trade_pcf.aspx` WebForms postback（`fund_id=23`） | `2026/08/07 每基數實際申購總價金` |
| 00407A | 凱基 | `src/kgi_scraper.py` | `/Fund/RedemptionVC` partial view（`fundID=J024`） | `(2026/08/07)每受益權單位淨資產價值` |
| 00410A | 永豐 | `src/sinopac_scraper.py` | `/SinopacEtfs/Etfs/Pcf/00410A` SSR | 頁面「資料日期：」 |

三家的頁面預設日期都是 **PCF 適用日（下一營業日）**，不是基準日——第 4～6 個同款案例。
細節與永豐「有 xlsx 下載卻不採用」的理由寫在 `.ai/guides/data-sources.md`，不在這裡重複。

已實跑的檢查（2026-08-09 15:56–15:59）：

- 三支 scraper 直接呼叫、**不寫 DB**：兆豐 50 筆、凱基 50 筆、永豐 29 筆，
  資料日期全部 `2026-08-07`（週五，正確）、`source_dated=True`（真的解析到來源日期）
- 權重合計 94.58% / 96.25% / 93.58%（都有期貨或現金部位，未達 100% 屬正常）
- 股數為 0、名稱為空、代號重複皆為 0
- 成分股名稱對照：129 筆持股的代號**全部**在 `data/stock_names.json` 內（查不到 0 筆）
- `python test_duplicate_guard.py` → **58/58 通過**（原 47 項＋新增 `check_batch3_parsers()` 11 項：
  三家的日期錨、找不到錨點／多個日期時的保守退回、兆豐排除期貨表、凱基排除合計列、
  永豐手機版重複表格不重複計算）
- **red-before 已證**：把三支的日期錨各自改成最可能犯的錯（兆豐改抓「查詢日期」、
  凱基改成泛抓任何日期、永豐改成照抄「每基數」錨），新測試立刻出現 4 個 FAIL；
  改回後恢復 58/58。永豐那條特別重要——它的頁面排列與兆豐／台新相反，
  「每申購基數」前面是**適用日**，照抄錨點就會錯位。
- `python main.py --help` 確認新增 `--megafunds` / `--kgi` / `--sinopac`，`--all` 也已串上

生產驗證已完成（run `31303353361`，`workflow_dispatch`，2026-08-09 08:22–08:26 UTC，全綠）：

- 守衛輸出 `今天(2026-08-09, UTC)已寫入=0/19 檔 / 跳過門檻=70%` → 正確放行
  （當時 `etf_list` 還是 19 檔）
- 三檔**首次寫入**：00996A 50 筆、00407A 50 筆、00410A 29 筆，資料日期皆 `2026-08-07`
- bot commit `059c2ab`；`etf_list` 現為 **22 檔**；持股總覽報表 `docs/data_2026-08-07.json`
  的 `etf_holdings` 為 22/22 檔，三檔 `data_date=2026-08-07`、股數已換算成「張」
- 同一 run 有兩檔**既有** ETF 失敗（00981A 無資料、00991A 復華 Excel 下載 Connection aborted），
  與本次變更無關，屬既有偶發，後備班次會再給來源一次機會

還開著的：

1. **變動追蹤要等下一個交易日**才會出現這三檔——首日沒有前一日可比，
   所以 08-07 報表的 `summary.total_etfs` 是 17（持股總覽仍是 22）。
2. **守衛分母已變 22**，70% 門檻換算為需 ≥16 檔。過去每日寫入 15–17/19，
   三檔來源穩定、預期落在 18–20/22，**但下一個自然班次要確認沒壓到門檻**。
3. 這份 diff **尚未獨立審查**——使用者 2026-08-09 決定先不做（非阻塞，記錄在此備查）。

## 目前在做什麼（B：日期錯位第二批）

**修日期錯位的根因**（任務 ID `TWETF-DATEFIX-20260805`）。當日 PCF 尚未發布時，
用請求日期當資料日期的 scraper 會把前一交易日的內容寫成當日。四個面向：

1. 寫入層加日期錯位防護，但只對「用請求日期」的來源生效（`source_dated` 豁免）。
2. 中信、富邦改用來源標示的資料日期；第一金／聯博／統一原本就是，改為明確宣告。
3. CI 早退守衛改看 `holdings.created_at` 覆蓋率，不看資料日期；並移除 push 觸發。
4. 報表**兩半**都逐檔回退到各自最新可得日期：持股總覽（`ReportManager`）與
   變動追蹤（`HoldingsAnalyzer.detect_changes_batch`）。少修一半就會讓落後的 ETF 消失。

設計理由寫在 `.ai/guides/date-alignment.md`，不要在這裡重複。

## ✅ 已完成：08-05 錯位資料搶救（2026-08-05 19:45–19:55 執行）

結果：**9 檔錯位全部救回真 08-05**（內容≠08-04）、08 月份錯位群組歸零、
持股總覽 19/19 檔、變動追蹤 15 檔。過程：

1. 刪 10 組 575 列（與演練逐字相同）→ commit `3109e80` → 守衛降到 5/19
2. `workflow_dispatch` 觸發 run `31002747343` → 全綠，**抓取路徑（守衛放行→爬蟲→
   淺歷史 commit/push）首次完整生產驗證** → bot commit `c9885c1`
3. 00406A 在該 run 暫時性失敗（No holdings data found；同輪 00995A 成功）。
   本機分兩步補救（`b7fa5a7`）：`daily_update_ctbc(generate_report=False)` 只寫 DB
   （56 筆、Excel 自標 08-05、source_dated=True），再 `generate_consolidated_reports()`
   重生完整報表。**教訓：絕不能用 `main.py --ctbc` 裸跑補單家，
   它會以只含該家變動的字典覆蓋整份日報。**
4. 旁證：中午被刪的 00406A 快照 57 筆 vs 今晚真 08-05 是 56 筆——內容確實不同。

殘餘：第一金兩檔（00408A/00994A）停在 08-04，其 08-05 依 T+1 節奏明晚自然補上，
**不需要任何動作**。當晚 22:2x 後備班次會看到 19/19 而跳過，屬預期。

同型事故日後的處理模板（本次驗證有效）：先刪錯位群組再重跑（不刪會留孤兒列，
`INSERT OR REPLACE` 只覆蓋同代號的列）；重跑必須在事故當日的 UTC 日內
（受防護來源的資料日期取 runner 的 `datetime.now()`，跨日會製造新錯位）；
單家補抓一律「只寫 DB ＋ `generate_consolidated_reports()`」兩步。

## 第二批（2026-08-05 晚間實作完成，待審查）

契約：`TWETF-BATCH2-20260805`（CRITICAL）。四家轉換＋UPSERT＋歷史清理全部落地：

| commit | 內容 | 驗證 |
| --- | --- | --- |
| `81c7f24` | 台新＋安聯轉 source_dated | 實站 27／133 筆、date=08-05、source_dated=True |
| `1985310` | 群益改走 buyback API（Excel 降備援） | 實站 00982A 55、00992A 37 筆；股數與權重和 DB 逐筆一致 |
| `16896c5` | 摩根估值日前瞻性＝新鮮度 | 實站 62 筆；今晚 VD=08-06 領先 → source_dated=True |
| `f402b2a` | UPSERT 保留 created_at＋守衛註解＋測試 31→47 | red-before 已證：舊實作（REPLACE）會刷新 created_at，新測試對它是紅的 |
| `d891a39` | 刪 52 組歷史錯位（3,234 列）＋重生 119 個已發布日期 | 刪除當下重新驗證清單、刪後全庫重掃歸零；抽驗 07-13 安聯正確回退 07-09、update_time 保留 |

**還開著的驗收項**：
1. 群益 `date2` 語意的盤中複測（明早 08-06 盤中打一次 API，`date2` 應仍為 08-05）
2. 觀察一個自然生產日（08-06 晚間班次）全綠
3. **兩份全新 session 的獨立審查**——本 session（a8dff6b3）是 builder，沒有資格

當初的調查結論（實作依據）留存如下：

晚間逐家實測（唯讀、不寫 DB）的結論：

| 來源 | 實測結果 | 第二批動作 |
| --- | --- | --- |
| 台新 | 頁面唯一日期樣態 `2026/8/5`（單位數月日）＝真資料日期；**`#PUB_DATE` 值是 2026-08-06（未來日）**，證實查詢框絕不可用 | 轉 `source_dated`，錨定「日期：」附近的日期，不碰 `#PUB_DATE` |
| 安聯 | 頁面明確標「資料日期 : 2026/08/05」 | 轉 `source_dated`（同富邦模式） |
| 群益 | 頁面與下載 Excel 皆無日期，**但 XHR 翻案**：`POST /CFWeb/api/etf/buyback {fundId}` 直接可用（免 Playwright、免認證），55 筆持股在 `.data.stocks`，`date1=下一交易日（PCF 適用日）`、**`date2=持股基準日`**——與摩根 PCF 同款前瞻日模式 | **可轉 `source_dated`**（用 `date2`）。轉換前明早盤中複測一次：屆時 `date2` 應仍為 08-05，即可定論欄位語意。順帶可讓群益甩掉 Playwright |
| 野村 | API 嚴格遵守 `SearchDate`：08-06（未發布）回空、08-04 歷史可查、回應會回帶資料日期 | 結構安全，免轉換；可選擇性解析回帶日期標 `source_dated`，消滅「同內容日誤擋」殘留風險 |
| 復華 | Excel API 嚴格遵守 URL 日期：08-06 回 12-byte JSON 錯誤、08-04 可查 | 同野村。這解釋了兩家歷史零錯位、以及 08-05 中午為何整批沒寫入（誠實回空） |
| 摩根 | PCF 的 `Valuation Date` 欄可讀到未夾住的原始值 | 實作 forward-date-aware 豁免：估值日有前進才視為更新（R2-F5 的方案） |
| 國泰 | （18:00 已測）已有真 08-05；API 無日期欄位、會靜默回退 | 維持防護，正確終態 |

歷史錯位（第二批的另一半）：

- 重掃全 DB：**歷史 52 組、橫跨 14 個交易日（2026-02-12 ~ 2026-07-31）**；
  今天 08-05 另有 9 組待搶救。（先前文件寫 61 組/15 日＝兩者合計，量測當下正確。）
- 分佈：安聯 19、群益 15、中信 7、台新 6、摩根 2、富邦 2、統一 1。
- **全部無法回填**：唯一支援歷史查詢的國泰（野村/復華也支援，但三家歷史零錯位）
  不在錯位名單裡；有錯位的七家來源都只提供最新檔。
- 因此歷史修復＝**純刪除**（內容與前一日逐列相同，刪除近乎資訊無損，
  報表層會逐檔回退並標 `data_date`）＋ `scripts/regenerate_reports.py` 重生歷史報表。
  刪除前要用同一套指紋邏輯重新驗證清單。

## 狀態

- 實作狀態：**ATTEMPT**（程式已寫、測試通過，但尚未獨立審查通過）
- 獨立審查：**尚未通過**（諮詢性狀態，不撤銷任何授權）
- 風險層級：CRITICAL（`behavior` / `persistence` / `external_side_effect` / `data_loss`）
- 第一輪審查（session `a94d238b`）開出 F1–F9，**F1–F4 已修**，但該 session 隨後
  參與修正，因此已轉為 builder，**不得**再擔任這份 diff 的獨立審查者。
- 需要 **2 份**全新 session 的獨立 receipt；重新 Freeze 後既有 receipt 全部失效。
- 凍結請求與 receipt 放在本機審查工具目錄（不進 repo）。

## 第二輪審查（session `a8dff6b3`）與修正

八項發現、加上修正過程中新冒出的一項，全部處理完畢：

| 編號 | 內容 | 處置 |
| --- | --- | --- |
| R2-F1 (P1) | 指南把已修好的兩半仍寫成「已知缺口」 | 改寫該段，換成真正的殘留項 |
| R2-F2 (P1) | `scripts/regenerate_reports.py` 自己組持股總覽，掉 `data_date` 且與變動追蹤日期規則不一致 | 抽出 `ReportManager.build_etf_holdings()` 當唯一出口，回填腳本改用；加防漂移測試 |
| R2-F3 (P2) | 停更日重複回報的變動被首頁重複計入買賣張數 | `detailed_changes` 帶 `data_date`；首頁**兩個**彙總面板都以 `(ETF, 資料日期)` 去重 |
| R2-F4 (P2) | 移除 push 觸發後，改程式碼沒有任何 workflow 會跑 | 新增 `.github/workflows/tests.yml`（push / PR 跑測試） |
| R2-F5 (P2) | 摩根那 3 次被稱為「誤擋」卻仍留在防護內，永久缺口未記為已接受成本 | 改寫：夾住後無法判讀，保留才正確；並記下每半年約 3 天、不會被後備班次補的代價 |
| R2-F6 (P3) | 測試用 `setdefault`，外部把旗標設成 False 會出現 4 個假失敗 | 改成強制指派；實測旗標設 False 仍 31/31 |
| R2-F7 (P3) | 前一交易日無資料時防護必然放行 | 記為指紋式設計的固有殘留風險 |
| R2-F8 (P3) | 後備排程註解寫「台灣時間 21:00」，實測 189 次執行全落在 UTC 14:21-16:25（台北 22:21 至隔日 00:25） | 改寫註解，並補上「日後調 cron 要預留這段延遲、別跨過 `00:00Z`」的理由（commit `4ac5ecf`） |
| R3-A (P3) | 去重讓合計正確了，但 `perDay` 仍以報表日期為 key，8/4 的調整會被畫在 8/6 的欄位 | 新增 `changeBucketDate()`，優先歸屬 `data_date`；資料日期落在顯示視窗外時退回報表日期，以免把顯示錯誤換成整筆消失。首頁兩處都改（commit `4ac5ecf`） |

R2-F3 修正時另外發現首頁有**第二個**彙總面板（跨 ETF 熱門股）有同樣的重複計入問題，
審查沒點到，一併修了。R3-A 則是在覆核 R2-F3 的修法時才發現的。

## 已實跑的檢查

- `python test_duplicate_guard.py` → **31/31 通過**（旗標預設與外部設成 False 都是 31/31），含：
  - 關閉旗標的 red-before 敏感性檢查
  - 報表層測試：落後的 ETF 必須同時活在持股總覽與變動追蹤兩半
  - 變動清單每筆帶 `data_date`、回填腳本共用 `build_etf_holdings` 的防漂移檢查
- 差異化驗證：同一支探測腳本在修 F1 前回傳 `['00981A']`（落後檔被剔除），
  修後回傳 `['00981A', '00995A']`
- CI 守衛 shell 片段原樣實跑：15-17/19 跳過、8/19 與 1/19 不跳過、無 DB 不跳過
- 歷史指紋模擬確認設計理由引用的數據正確（15 個錯位交易日、最嚴重 9 檔等）
- `docs/index.html` 以 `node --check` 驗過 JS 語法
- **實站驗證（2026-08-05 16:44–17:25）**：五支 source_dated scraper 全部直接呼叫、不寫 DB，
  全部 `source_dated=True`（真的解析到來源日期，不是退回請求日期）：

  | scraper | 筆數 | 資料日期 |
  | --- | --- | --- |
  | 中信 00995A | 54 | 2026-08-05 |
  | 富邦 00405A | 49 | 2026-08-05 |
  | 第一金 00994A | 38 | **2026-08-04** |
  | 聯博 00404A | 50 | **2026-08-04** |
  | 統一 00403A | 50 | 2026-08-05 |

  第一金與聯博當下確實落後一天，代表兩半的逐檔回退在今天就會實際用到。
- **Cloudflare 觸發方式已確認**：`C:\GIT\gh-cron-trigger\worker.js` 打的是
  `workflow_dispatch` API，不是 push。移除 push 觸發不會讓主班次停擺。

## 一個被實測推翻的前提

原本各處註解寫「中信、富邦只揭露到前一交易日」。16:44 實測兩家都已有**當日**資料。
正確的說法是「當日 PCF 的發布時間各家不同，盤中抓到的常常還是前一交易日」——
這正是盤中 push 觸發會寫錯資料、而收盤後排程沒事的原因。已修正 9 處敘述。

## 待處理

- **獨立審查要重跑**：改動過了，先前所有 receipt 一律失效。CRITICAL 需 2 份全新 session、
  建議至少一份換模型。`2d5c3882` 與 `a94d238b` 都因參與修正而沒有資格。
- ~~真實 GitHub Actions 執行~~ **兩條路徑都已在生產驗證完畢**：跳過路徑
  （run `30997358757`：15/19 正確跳過、無 commit）＋ 抓取路徑
  （搶救 run `31002747343`：5/19 放行 → 爬蟲 → 淺歷史 commit/push 全綠）。
  這個驗收項結案。
- **歷史髒資料**已明確列為不在範圍。審查者仍會把它列為 unverified，
  要讓 gate 通過同樣需要使用者授權 waiver。
- ~~推送可能被 GitHub 擋下（憑證缺 `workflow` scope）~~ **已被實測推翻**：
  2026-08-05 兩次帶 workflow 檔案變更的 push（`ce9e0ba` 改 daily-scraper.yml＋新增
  tests.yml、`4ac5ecf` 改 daily-scraper.yml）都成功，憑證有 `workflow` scope。
  `.gitattributes` 裡為此而設的排除規則已依其自身註記（「upgraded 後移除」）拿掉。
- 已知取捨（非阻塞）：見 `.ai/guides/date-alignment.md` 的「已知取捨與殘留風險」。

## 其他待辦（與上面任務獨立）

- ~~還缺 3 檔純台股主動式 ETF 未追蹤~~ → **2026-08-09 已補完**（見上方「第三批」一節）。
  盤點方法見 `.ai/guides/adding-an-etf.md`；2026-08-09 重查仍是 29 檔 `00xxxA`、無新掛牌。
- 00412A 安聯亞洲半導體尚未掛牌，標的非純台股，要不要收錄待決定。
- ~~歷史髒資料沒有修復計畫~~ → **已排入第二批**（見上面「第二批」一節）：
  歷史 52 組純刪除＋重生報表；今天的 9 組走明早搶救。
